const accountNum = 2
let accountName = "Jannat"
var accountState = "delhi"
accountCountry = "India"

console.log(accountNum);
console.log([accountName, accountState, accountCountry]);


/*
const = use when you don't want to change the value of variable, it is important to initialize the variable while declaring it.
let and var = you can use their values, There is the scope problem in var which is solved by let.
if we dont initialize let or var it gives undefined value.
*/